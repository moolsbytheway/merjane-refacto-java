spring:
  liquibase:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate.format_sql: true
  main:
    banner-mode: off

server:
  servlet:
    context-path: /api

logging:
  level:
    org.springframework: ERROR
    root: ERROR
